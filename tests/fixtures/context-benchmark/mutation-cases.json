{
  "schema": "repoctl.context.benchmark.mutations",
  "schema_version": 1,
  "cases": [
    {
      "id": "rename-source",
      "kind": "rename",
      "description": "source refs must detect renamed documents"
    },
    {
      "id": "digest-drift",
      "kind": "source_digest_drift",
      "description": "source refs must surface changed content digests"
    },
    {
      "id": "multi-repo-topic-collision",
      "kind": "multi_repo_isolation",
      "description": "same topic text in two repos must not merge"
    },
    {
      "id": "import-impact-resolution",
      "kind": "code_impact_resolution",
      "description": "repo-local Python and JS/TS imports should surface importer files in impact questions"
    },
    {
      "id": "same-file-reference-impact",
      "kind": "reference_impact_resolution",
      "description": "same-file caller symbols are measured separately from import impact"
    },
    {
      "id": "same-class-method-impact",
      "kind": "method_call_impact_resolution",
      "description": "same-class self.method calls should be measured separately from module-level function calls"
    },
    {
      "id": "cross-file-imported-call-impact",
      "kind": "cross_file_call_impact_resolution",
      "description": "cross-file imported function calls should be measured only after import resolution and stable symbol identity exist"
    },
    {
      "id": "structured-runtime-file-impact",
      "kind": "structured_file_relation_resolution",
      "description": "Docker, Compose, workflow, and shell references should resolve only through explicit file syntax"
    },
    {
      "id": "sql-owner-impact",
      "kind": "sql_owner_relation_resolution",
      "description": "seed and client RPC references should resolve to the unique SQL owner definition"
    }
  ]
}
