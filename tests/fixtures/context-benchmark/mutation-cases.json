{
  "schema": "repoctl.context.benchmark.mutations",
  "schema_version": 1,
  "cases": [
    {"id": "rename-source", "kind": "rename", "description": "source refs must detect renamed documents"},
    {"id": "digest-drift", "kind": "source_digest_drift", "description": "source refs must surface changed content digests"},
    {"id": "multi-repo-topic-collision", "kind": "multi_repo_isolation", "description": "same topic text in two repos must not merge"}
  ]
}
