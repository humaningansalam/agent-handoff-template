[project]
name = "agent-handoff-template-workspace"
version = "0.2.60"
description = "Workspace maintenance tooling for the agent handoff template"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false

[dependency-groups]
dev = [
  "pytest>=9.0.3"
]
