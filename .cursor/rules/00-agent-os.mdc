---
description: Agent OS routing rule (thin shim)
globs:
  - "**/*"
alwaysApply: true
---

1) Read root `AGENTS.md` first and follow it as the shared contract.
2) Then follow the read order and task rules defined in `AGENTS.md`.
3) If this file conflicts with `AGENTS.md`, `AGENTS.md` wins.