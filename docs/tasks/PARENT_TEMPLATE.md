---
id: T-YYYYMMDDHHMMSSZ
title: "Replace with parent task title"
# canonical values: todo | doing | blocked | done | canceled (see AGENTS.md)
status: todo
owner: "unassigned"
# optional future branch/worktree hint; never used as repository selector
repo_ref: ""
# optional stable product repository id; child tasks usually own executable repo scope
repo_id: ""
created: YYYYMMDDTHHMMSSZ
# optional: frontend | backend | infra | docs | ops
area: ""
# parent tasks should leave this empty
parent: ""
# optional: advisory prerequisite task IDs (not enforced)
depends_on: []
---

# T-YYYYMMDDHHMMSSZ - Parent Title

## Context Docs

<!-- List only the minimum context docs needed for this task.
This section may be empty.

Example:
- `docs/PRD.md`
- `docs/workflows/repo-metadata.md`
-->

## Work Area

- Task record: `docs/tasks/T-YYYYMMDDHHMMSSZ--slug.md`
- Primary surface: Identify child task surfaces before implementation starts.
- Area hint: none provided

## Goal

State the cross-cutting outcome in one clear sentence.

## In Scope

- List concrete deliverables
- Keep items testable

## Out of Scope

- List explicit non-goals
- Prevent scope drift

## Plan

- Step 1
- Step 2
- Step 3

## Live Child Tasks

<!--
This list is a coordination summary, NOT the authoritative source.
The child's `parent` frontmatter field is the authoritative link.
If this list conflicts with a child's frontmatter, the child wins.
Update on a best-effort basis; omissions are not rule violations.
-->
- `docs/tasks/T-YYYYMMDDHHMMSSZ--child-task.md`

## Non-Live Child Tasks

- `docs/tasks/T-YYYYMMDDHHMMSSZ--non-live-child-task.md`

## Shared Interfaces / Decisions

- Decision 1

## Integration Done When

- Condition 1

## Execution Log

<!-- Append only at meaningful checkpoints. Prefer `./scripts/repoctl task log append T-... "message" --json` so UTC timestamps are generated by repoctl. -->

## Verification

- Command(s) run
- Evidence captured
- Result

<!-- Finish with: `./scripts/repoctl task finish T-... --verification-file /tmp/T-...-verification.md --json` -->

## Handoff

- Next exact step: Check child task statuses and update the coordination summary.
- First file to open: `docs/tasks/T-YYYYMMDDHHMMSSZ--parent-title.md`
- First command to run: `rg "^parent:" docs/tasks/`
- Done when: The parent task reflects the latest child state (including non-live tasks) and the next agent can continue without guessing.

<!--
Use this template only when the work needs a coordinating parent task across multiple narrower child tasks.
Child tasks should use `docs/tasks/TEMPLATE.md` with `parent: "T-..."` filled in.
The child's `parent` field is the authoritative link; this file's child lists are convenience summaries.
-->
